#!/bin/sh

command="${1}"

case "${command}" in
   "start") sh /etc/shunt/start.sh
         ;;
   "stop") sh /etc/shunt/stop.sh
         ;;
   *) echo "[info] Usage: /etc/init.d/shunt-daemon {start|stop|restart}."
      ;;
esac
